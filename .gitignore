# ── Secrets / credentials (절대 커밋 금지) ──
data/.secrets.json
.secrets.json
.env
.env.*
*.env

# ── Dependencies ──
node_modules/
.pnpm-store/

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# ── Build output ──
dist/
build/
out/
src-tauri/target/
*.tsbuildinfo

# ── Models / large caches (HuggingFace, GGUF 등 — 직접 다운로드) ──
models/
*.gguf
*.safetensors

# ── Logs ──
*.log

# ── Runtime / user data (개인 데이터·캐시) ──
data/chroma/
data/history/
data/autosave/
data/uploads/
data/marketplace_cache/
data/training/
# 벤치·데모 산출물 + 실명 PII(채용점수표) — 커밋 금지
data/fixtures/

# ── Installers / binaries ──
*.msi

# ── Reverse-engineering / 참조 덤프 (비공개) ──
inline structure/
document_extractor_full.txt
hwp_editor_full.txt

# ── OS / editor ──
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# 모델 비교 원장 (로컬 실측 로그)
scripts/benchmark_ledger.jsonl
scripts/model_matrix_result.json
