{
  "$schema": "https://raw.githubusercontent.com/nicholasgasior/tauri/dev/crates/tauri/capability-schema.json",
  "identifier": "default",
  "description": "기본 권한 — 창 관리, 셸 실행",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:allow-execute",
    "shell:allow-spawn",
    "shell:default"
  ]
}
